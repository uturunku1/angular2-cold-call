import { CallFilterPipe } from './call-filter.pipe';

describe('CallFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CallFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
